{
  "name": "pokemon-black-crystal",
  "version": "3.0.00",
  "private": true,
  "type": "module",
  "scripts": {
    "pwa-assets": "pwa-assets-generator",
    "build": "pwa-assets-generator && nuxt build",
    "dev": "nuxt dev --host",
    "generate": "nuxt generate",
    "preview": "npm run build && node .output/server/index.mjs",
    "postinstall": "nuxt prepare"
  },
  "dependencies": {
    "nuxt": "^3.12.4",
    "vue": "latest",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@vite-pwa/assets-generator": "^0.2.4",
    "@vite-pwa/nuxt": "^0.9.1",
    "sass": "^1.77.8"
  }
}
